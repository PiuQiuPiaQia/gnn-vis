# GraphCast 数据预处理

本文件夹说明 GraphCast 模型的数据预处理流程和数据格式。

## 📊 预处理数据说明

GraphCast 预处理数据存储在 `../graphcast-data/` 目录中：

| 文件 | 说明 |
|------|------|
| `_mesh_nodes_lat.pkl` | Mesh 节点纬度坐标 |
| `_mesh_nodes_lon.pkl` | Mesh 节点经度坐标 |
| `eval_inputs.pkl` | 模型输入数据（气象变量） |
| `eval_forcings.pkl` | 强迫数据（边界条件） |
| `eval_targets.pkl` | 真实目标值（用于评估） |
| `predictions.pkl` | 模型预测结果 |
| `grid_node_features.pkl` | 网格节点特征 |
| `latent_mesh_nodes.pkl` | 潜在 mesh 节点特征 (1038240, 1, 471) |
| `updated_latent_mesh_nodes.pkl` | 更新后的潜在节点 |

---

## 🔄 数据预处理流程

```
┌─────────────────┐
│   ERA5 原始数据  │  (WeatherBench2)
└────────┬────────┘
         ↓
┌─────────────────┐
│   数据标准化     │  (归一化、时间特征)
└────────┬────────┘
         ↓
┌─────────────────┐
│  构建图结构      │  (网格 + Mesh)
└────────┬────────┘
         ↓
┌─────────────────┐
│  节点/边特征     │  (空间特征、时间编码)
└────────┬────────┘
         ↓
┌─────────────────┐
│  模型推理        │
└────────┬────────┘
         ↓
┌─────────────────┐
│  预测结果输出    │
└─────────────────┘
```

---

## 📦 数据格式

### 输入数据 (eval_inputs.pkl)
包含 18 个气象变量：
- 温度：`2m_temperature`, `temperature`
- 风速：`10m_u_component_of_wind`, `10m_v_component_of_wind`, `u_component_of_wind`, `v_component_of_wind`
- 湿度：`specific_humidity`
- 气压：`mean_sea_level_pressure`, `geopotential`
- 其他：`total_precipitation_6hr`, `vertical_velocity`, 等

### 时间特征
- `year_progress_cos/sin` - 年度周期
- `day_progress_cos/sin` - 日周期
- `toa_incident_solar_radiation` - 太阳辐射

### 潜在表示 (latent_mesh_nodes.pkl)
- 形状：`(1038240, 1, 471)`
- 471 维特征向量，来自 GraphCast 编码器

---

## 🔧 数据生成代码

数据预处理相关代码位于 `../GraphCast-from-Ground-Zero/`：

| 文件 | 功能 |
|------|------|
| `graphcast/data_utils.py` | 数据加载和预处理 |
| `graphcast/grid_mesh_connectivity.py` | 构建 grid-mesh 图结构 |
| `graphcast/icosahedral_mesh.py` | 二十面体网格生成 |

---

## 📖 相关资源

- [GraphCast 论文](https://arxiv.org/abs/2212.12794)
- [WeatherBench2 数据集](https://github.com/google-research/weatherbench2)
- [ERA5 数据文档](https://www.ecmwf.int/en/forecasts/datasets/reanalysis-datasets/era5)
